<!DOCTYPE html>
<html lang="it">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewData["Title"] - GestioneFacile ;)</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&family=Nunito:wght@400;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Nunito', sans-serif;
        }
        h1, h2, h3 {
            font-family: 'Poppins', sans-serif;
        }
        
        .nav-link::after {
            content: '';
            display: block;
            width: 0;
            height: 2px;
            background: linear-gradient(90deg, #0f0f29, #04000c);
            transition: width 0.3s;
        }
        .nav-link:hover::after {
            width: 100%;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-blue-50 via-blue-100 to-blue-200 text-gray-800 flex flex-col min-h-screen">

    <nav class="bg-blue-900 backdrop-blur-md shadow-sm fixed top-0 left-0 w-full z-20 border-b border-gray-100">
        <div class="container mx-auto px-6 py-4">
            <div class="flex justify-between items-center">
                <a href="/" class="text-2xl font-bold gradient-text text-white">GestioneFacile</a>
                <div class="hidden sm:flex space-x-8 items-center">
                    <a asp-page="/Dashboard/Index" class="nav-link text-white font-medium transition-colors duration-300">Dashboard</a>
                    <a asp-page="/Clienti/Index" class="nav-link text-white font-medium transition-colors duration-300">Clienti</a>
                    <a asp-page="/Ordini/Index" class="nav-link text-white font-medium transition-colors duration-300">Ordini</a>
                </div>
                <button id="mobile-menu-btn" class="sm:hidden p-2 rounded-lg hover:bg-gray-100">
                    <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"/>
                    </svg>
                </button>
            </div>
            <div id="mobile-menu" class="sm:hidden hidden mt-4 space-y-2 transition-all duration-300">
                <a asp-page="/Dashboard/Index" class="block px-4 py-3 hover:bg-gray-50 rounded-lg transition-colors duration-300">Dashboard</a>
                <a asp-page="/Clienti/Index" class="block px-4 py-3 hover:bg-gray-50 rounded-lg transition-colors duration-300">Clienti</a>
                <a asp-page="/Ordini/Index" class="block px-4 py-3 hover:bg-gray-50 rounded-lg transition-colors duration-300">Ordini</a>
            </div>
        </div>
    </nav>

    <main class="container mx-auto px-6 py-12 flex-grow mt-20">
        @RenderBody()
    </main>

    <footer class="border-t  bg-blue-900 backdrop-blur-md mt-20">
        <div class="container mx-auto px-6 py-8">
            <div class="flex flex-col items-center">
                <div class="flex space-x-6 mb-6">
                    <a href="https://www.facebook.com" class="p-2 rounded-full bg-amber-400 hover:bg-gradient-to-r from-indigo-500 to-purple-600 hover:text-white transition-all duration-300">
                        <i class="fab fa-facebook-f"></i>
                    </a>
                    <a href="https://www.twitter.com" class="p-2 rounded-full bg-amber-400 hover:bg-gradient-to-r from-indigo-500 to-purple-600 hover:text-white transition-all duration-300">
                        <i class="fab fa-twitter"></i>
                    </a>
                    <a href="https://github.com/Giovanni-Colabella" class="p-2 rounded-full bg-amber-400 hover:bg-gradient-to-r from-indigo-500 to-purple-600 hover:text-white transition-all duration-300">
                        <i class="fab fa-github"></i>
                    </a>
                </div>
                <p class="text-white text-sm text-center mb-2">
                    &copy; 2025 GestioneFacile - Tutti i diritti riservati.<br>
                    <span class="text-white">Sviluppato da &copy;Giovanni Colabella</span>
                </p>
            </div>
        </div>
    </footer>

    <script>
        const menuBtn = document.getElementById('mobile-menu-btn');
        const mobileMenu = document.getElementById('mobile-menu');
        let menuOpen = false;

        menuBtn.addEventListener('click', () => {
            menuOpen = !menuOpen;
            mobileMenu.classList.toggle('hidden');
            menuBtn.classList.toggle('text-indigo-600');
            menuBtn.querySelector('svg').style.transform = menuOpen ? 'rotate(90deg)' : 'rotate(0)';
        });
    </script>

</body>
</html>