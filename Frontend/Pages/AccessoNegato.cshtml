@page
@model Frontend.Pages.AccessoNegatoModel

@{
    Layout = "_LayoutHome";
}

<div class="fixed inset-0 flex items-center justify-center min-h-screen">
    <div class="bg-white/80 backdrop-blur-lg p-10 rounded-2xl shadow-xl border border-gray-200 text-center max-w-md">
    <i class="fa-solid fa-lock fa-3x text-center"></i>
        <div class="flex justify-center mb-4">
        </div>
        <h1 class="text-3xl font-extrabold text-gray-900">Accesso Negato</h1>
        <p class="text-gray-700 mt-3">Non hai i permessi necessari per accedere a questa pagina.</p>
        <a asp-page="/Index" class="mt-6 inline-block px-5 py-2.5 text-lg font-semibold bg-red-600 text-white rounded-lg hover:bg-red-700 transition-all shadow-md">
            <i class="fa-solid fa-arrow-left mr-2"></i> Torna alla Home
        </a>
    </div>
</div>